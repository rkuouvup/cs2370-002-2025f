// get a string input from the user
// count how many uppercase letters are in the string
// print the number
// <cctype> library
// isupper(character) ==> return true or false
#include <iostream>
using namespace std;

/*int main() {
    string s;
    int count = 0;
    
    cout << "Please enter a string: ";
    getline(cin, s);
    
    //for (int i = 0; i < s.length(); i++) {
    //    if (isupper(s.at(i)))
    //        count++;
    //}
    
    for (auto c : s) {  // for c in s
        if (isupper(c))
            count++;
    }
    
    
    cout << "There are " << count << " uppercase letters" << endl;
    
    return 0;
}*/
